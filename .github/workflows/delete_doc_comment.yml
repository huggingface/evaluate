name: Delete dev documentation

on:
  pull_request:
    types: [ closed ]


jobs:
  delete:
    uses: huggingface/doc-builder/.github/workflows/delete_doc_comment.yml@06d4dc06c4e3f347dde2d716120f813db727a5da  # main
    with:
      pr_number: ${{ github.event.number }}
      package: evaluate